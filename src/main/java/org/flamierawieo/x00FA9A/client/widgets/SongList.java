package org.flamierawieo.x00FA9A.client.widgets;

import org.flamierawieo.x00FA9A.Images;
import org.flamierawieo.x00FA9A.client.ui.Widget;
import org.newdawn.slick.GameContainer;
import org.newdawn.slick.Graphics;
import org.newdawn.slick.Image;

public class SongList extends Widget {

//    private Image songListBackgroundImg, activeSongBackgroundImg, searchBackgroundImg;

    public SongList(float x, float y, float w, float h, float originX, float originY) {
        super(x, y, w, h, originX, originY);

//        songListBackgroundImg = Images.SONG_LIST_BACKGROUND.getImage();
//        activeSongBackgroundImg = Images.ACTIVE_SONG_BACKGROUND.getImage();
//        searchBackgroundImg = Images.SEARCH_BACKGROUND.getImage();

    }

    @Override
    public void draw(Graphics g) {
//        GameContainer container = getContainer();
//        g.drawImage(songListBackgroundImg, 0.0f, 0.0f);
//        g.drawImage(activeSongBackgroundImg, 0.0f, 0.0f);
//        g.drawImage(searchBackgroundImg, 0.0f, 0.0f);
    }

}
